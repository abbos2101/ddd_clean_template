Update the pubspec.yaml dependencies to their Resolvable versions based on the outdated report below. Rules:
1. Only update packages that have a Resolvable version different from Current
2. Keep everything else exactly the same (formatting, order, comments)
3. Remove * prefix from version numbers (e.g., *6.11.2 → 6.11.2)

Outdated report:
[paste the flutter pub outdated output]

Current pubspec.yaml:
[paste your pubspec.yaml]